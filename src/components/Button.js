import React from 'react';
import '../App.css'

const Button = ({ buttonTitle }) => {
    return (
        <div>
            <button >{buttonTitle}</button>
        </div>
    )
}

export default Button
